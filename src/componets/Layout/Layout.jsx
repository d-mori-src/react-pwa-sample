export const Layout = ({children}) => {
    return (
        <div className="container">
            <header>
                {/* header */}
            </header>
                {children}
            <footer>
                {/* footer */}
            </footer>
        </div>
    );
}